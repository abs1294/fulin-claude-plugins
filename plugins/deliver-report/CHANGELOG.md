# Changelog

本檔記錄 deliver-report 的版本變更，格式依 [Keep a Changelog](https://keepachangelog.com/)。

## [0.9.1] - 2026-08-30
### Fixed
- 修正「八條鐵則」的過時說法：`references/document-readability.md` 正本實際有 **12 條**（檔案開頭自述「這十二條就適用」），但外部引用仍寫八條——鐵則 9~12（視覺標記成套一致／能腳本化就不叫人手動／改文件前先讀實際樣貌／做完的事寫成做完）是後續踩坑陸續補上的，引用處沒跟著更新。
- 掃全 repo 後共修 6 處：`marketplace.json`、`plugin.json`、plugin `README.md`、`skills/deliver-report/SKILL.md`（frontmatter + 內文各一）、`skills/test-report-docx/SKILL.md`、根 `README.md`。
- **未改**正本第 339 行的「這八條」——該處是歷史敘述（記錄初版 8 條的由來與使用者原話），不是現況宣稱。「五項機械掃描」經核對與正本鐵則 8 的「五項必掃」一致，無需更動。

## [0.9.0] - 2026-08-29
### Added
- **併入 daily-report 成為第三個 skill**（`skills/daily-report/`，plugin 名維持 `deliver-report`）。三個 skill 都是「把工作成果變成對外產物」：交付訊息、測試報告 DOCX、工作日報。
- 併入理由：日報同樣是**對外產物**，同樣不能出現 AI／工具鏈字眼與異動紀錄用語——daily-report 自己就有 `content_guard.py` 硬閘（其 docstring 記載首次寄出的日報含 9 處洩漏）。規則同源，該放一起管。
- scope 由 user 改為 project：查證 `extract_sessions.py` 後確認日報**預設就是掃單一專案**（`--all-projects` 才掃全機），本來就是 per-project 設計，user scope 只是安裝位置而非功能需求。

### Notes
- daily-report 的腳本路徑全走 `${CLAUDE_PLUGIN_ROOT}/skills/daily-report/scripts/`，skill 資料夾名不變故無需改寫；腳本間 import 走 `__file__` 相對路徑，`check_no_secrets.py` 的 repo 上溯深度亦不變。
- 設定檔位置不變（`~/.claude/daily-report/`、專案層 `.claude/daily-report.json`），既有使用者的 OAuth／SMTP 設定不受影響。
- **兩份禁字實作仍是兩份**：`content_guard.py`（Python 掃 .md）與 `doc-readability-gate.js`（Node 掃 .docx）掃描對象與時機不同，併 plugin 不會自動去重。後續若要單一事實來源，需另抽資料檔讓兩邊讀。


## [0.8.0] - 2026-08-29
### Changed
- **拆成兩個 skill**：`deliver-report`（交付訊息）與 `test-report-docx`（測試報告 DOCX）。原本測試報告是主 skill 的一個「模式」、靠 `references/test-report-docx.md` 分流——但那個分流是 prompt 層的自律指令（「請讀 references/…」），沒有機制保證會被執行；且寫報告時仍全載主檔 511 行，其中約 320 行（四個決定／三種形狀／四個範例）完全用不到。改成獨立 skill 後，分流交給 harness 的 skill 選擇機制，各自只載需要的。
- **`document-readability.md` 上移至 plugin 層級**（`plugins/deliver-report/references/`），兩個 skill 以 `../../references/` 共用同一份，不複製、不漂移。本 repo 有前例：plugin-manager 的 skill 以 `../../CONVENTIONS.md` 引用 plugin 層級檔案。
- **Stop hook 的 skill 偵測改成明確清單**（`GATED_SKILLS` + `isGatedSkill()`）。舊版 `indexOf('deliver-report')` 是子字串比對，新 skill 命名為 `test-report-docx`（不帶前綴）時完全比不中——閘門看起來還在、實際不觸發，是最糟的失敗模式。新版支援裸名、`plugin:skill` 前綴、`/` 分隔三種形式。
- 主 skill 移除「模式分流」整節，改為一句判準（對方收到的是文字還是檔案）＋指向 `test-report-docx`；`test-report-docx` 內原有七處「主文」相對稱謂改為明確指名 skill。

### Verified
- Hook **實跑驗證**（非僅語法檢查）：以合成 transcript ＋ 含鐵則 5 禁字的 .docx 實測五種情境——`deliver-report`／`test-report-docx`／`deliver-report:test-report-docx` 三者皆正確 block，`git-commit` 與「本回合未調用 skill」皆正確放行。

## [0.7.0] - 2026-08-25
### Added
- **新增 Stop hook `doc-readability-gate.js`**：把 document-readability.md 中「機器判得準」的鐵則做成交付前機械閘（純 prompt 規範擋不住——實證：該文件寫完鐵則 8「修完一類要全文重掃」後，作者接著又在同一批文件犯了三次同類問題）
  - **會擋**：鐵則 3 編號缺號／小數點式編號、鐵則 4 未定義代號（§N / #N / S,V）、鐵則 5 異動紀錄用語、鐵則 8 交叉引用失效、鐵則 9 樣式一致性（⚠※◆★ 樣式分歧、主標字級小於子標、節標題顏色不一）、鐵則 9d 表格窄欄塞長字
  - **只提醒不擋**：鐵則 1／2／6／10（需語意理解，機器判不準）
  - FAIL-OPEN：任何讀檔失敗或例外一律放行；同一 session 最多擋 2 次避免迴圈
  - 實測：對含已知缺陷的舊版正確擋下（⚠ 5 種樣式、主標 18 < 子標 26、節標題 3 種顏色、表格窄欄 16 字），對修正後的現行文件正確放行

## [0.6.0] - 2026-08-25
### Added
- `document-readability.md` 新增**鐵則 9：視覺標記要成套且一致**——括號符號只能有一種用途（實案：`【】` 同時當等級／標題／嚴重度用，等級本身有 8 種寫法）；四種標記各司其職並在文件開頭給圖例（⚠ 陷阱紅字／◆ 這裡要做選擇／※ 備註灰字小一級／▼▶ 指令）；標題階層字級遞減顏色一致（實案：主標 sz24 比子標 sz26 還小）；表格窄欄不塞長文字（實案：2268 dxa 放 26 字擠成直排）
- 新增**鐵則 10：能寫成腳本的就不要叫人手動做**——三/四支腳本標準結構（precheck／apply／apply_db／verify）、哪些不該自動化（會靜默毀資料的序列化欄位、需對方帳號、牽涉判斷）、**腳本沒實跑過不算交付**（附實跑抓到的 6 個真 bug）
- 鐵則 8 的機械掃描 5 項 → **7 項**（補樣式一致性、表格欄寬）；SKILL.md 自檢章節同步

### Changed
- 開頭總綱補上一段自白：初版寫完鐵則 8「修完一類要全文重掃」後，作者接著又在同一批文件犯了三次同類問題才補出鐵則 9——**知道規則不等於做得到，所以要求的是機械掃描而非自律**

## [0.5.0] - 2026-08-25
### Added
- 新增 `references/document-readability.md`：交付文件易讀性八條鐵則（不要兩邊對照／同一件事放一起／編號＝執行順序不得有小數點缺號／不得出現未定義代號／不寫異動紀錄用語／能自查不丟給讀者／不重複不冗長／交付前全文機械掃描），每條附使用者實案原話
- SKILL.md 新增「第三步之二：交付前自檢」：五項機械掃描（符號代號／編號連續性／交叉引用／重複偵測／異動紀錄用語）＋跨文件一致性；含三個 .docx 實作陷阱（zipfile 驗證不足須加 XML parse 與 Word 實開、編號替換被 TLS1.0 咬字、檢查腳本自己過時）

### Fixed
- **測試報告編號改純序號，移除 A-/B-/C- 前綴規定**（與 0.4.0 條款衝突）：使用者問「A-N 是啥」＝沒過關。圖說格式同步改為「對應第 N 項「情境名」」，讓讀者不必回頭查表
- 來源：單一專案 20 次易讀性指正，歸為 7 類且**沒有一類只講一次**——因為當時只修被指的那一處、未做同類全掃

## [0.4.4] - 2026-08-18
### Fixed
- 測試報告誠實邊界改制：測試限制/缺陷/後續建議不進交付檔，改交付前對使用者揭露；缺陷先修再交付

## [0.4.3] - 2026-08-18
### Fixed
- 測試報告交付位置改為只放專案內慣用路徑，不主動複製到桌面（使用者否決桌面副本）

## [0.4.2] - 2026-08-13
### Fixed
- 測試報告模式補鐵則 7、8：不寫我方修了什麼 bug（缺陷修復章/既有回歸列一律不進報告，除非交付物本身就是缺陷修復說明）；測試表宣稱須逐項對得上實際檢核（不可用畫面欄位數充當驗證數）。並修正鐵則 6 邊界條款——原寫「修正前後屬交付物內容要保留」與鐵則 7 衝突

## [0.4.1] - 2026-08-13
### Fixed
- 測試報告模式補鐵則 6：只寫最終狀態、不寫我方作業時序（禁字：複驗/重跑/補行/第 N 輪/報告產出前/當時）；並標明「修正前後」屬交付物內容不在禁列，判準為「講系統狀態還是講我做了什麼」

## [0.4.0] - 2026-08-12
### Changed
- 新增測試報告 DOCX 輸出模式：references/test-report-docx.md——與主文同哲學，不定固定格式，改為「組裝決定 ×5＋段落構件庫」按需組裝（白話對照/測試表值層級證據/實機截圖/資料修正紀錄/測試限制/執行程序），加鐵則（禁字清單/meta 句不寫/誠實邊界）與 python-docx 技術要點；SKILL.md 加模式分流與觸發詞

## [0.3.5] - 2026-07-27
### Fixed
- **分級額度給錯密度**（0.3.4 的 40 字/檔實測是「一行一檔」的密度 38，而決定一「主體」要求的「展開子項」實測是 88 字/檔）：等於額度只夠買規則自己拿來當降級懲罰的那種寫法，主體信只要檔案一多就仍然只能全壓成一行，把「主體／佐證」的差別抹平。改為**一行帶過加 40、要展開子項加 90**，並要求先決定哪幾份要展開再算額度（只有對方要拿去審/簽核/照著做的才展開）
- **修「順位 1、2 永遠不會消失」與砍法第 4 階的直接矛盾**：第 4 階原寫「只留最嚴重的一項＋一句其餘 N 項在第 N 章」＝一句話蓋掉 N 項，與「每項壓成半句、不會整項不見」對同一個超限狀態給出相反承諾。改為第 4 階只砍順位 4、5，新增第 5 階「一句總述＋點名最嚴重的幾項＋其餘計數指路」，底線是對方知道「有這類問題、有幾項、在哪一章」，不會以為不存在
- **補「一件事同時符合多個順位」的規則**：取數字最小者。例「舊版金鑰 8/31 停用」既是分眾共通項（順位 3）又是「不換就連不上」（順位 1）→ 算順位 1 絕不砍；原本兩種歸法導出相反砍法，且位置規則說它必須獨立存在、優先序規則卻允許砍掉它
- **補字數的計數範圍**（原本完全未定義）：只數中文字，檔名/網址/英文/數字不計（範例一含檔名共 774 字元、中文 473，上限對的是後者）；分眾信三區合計。並補可操作的代理指標——精確計數不是重點，用「是否明顯比範例長」與行數（範例 12/16/14 行，超過 20 行就太長）判斷
- **400 字檢查點改為「七成」**：0.3.4 把上限改成浮動（550＋分級額度）後，固定的 400 沒跟著浮動——20 檔時 400/1150 只有 35%，信寫三分之一就被要求停下來檢查，連續誤報會讓檢查點被整體忽略
- **形狀 C 骨架的「最多一到兩句」與三句常態上限打架**：骨架比規則更嚴，而骨架才是實際照抄的那個。改為標明「＝常態三句；遇前提校正或對方要拿去報預算的數字可到五句」
- **移除三個形狀標題的決定組合標籤**（「形狀 A：逐檔導讀（檔案主體 + 無提問 + 單受眾）」→「形狀 A：逐檔導讀」）：標籤把「四個決定 → 形狀」變成可查表的映射，讀者定完決定後掃標題括號找相符的就照抄骨架——正是文件在範例區明文禁止的「比對標籤挑最接近的照抄」，卻在形狀區留了同一個門。改為敘述性說明並註明「不要拿括號當索引照抄」

- **堵安全性「另案建議」的逃逸路徑**：原條款用「與本次交付有無因果關係」當門檻，但因果關係取決於對方交付後會怎麼動系統——那正是我方不知道的事，且判斷權落在有動機降級的一方。改為舉證責任反轉（**說得出「交付前後完全一樣、且對方照這次內容操作不會碰到」才算無關，說不出來就進信**），並明訂三種一律不得歸另案：①本次交付的文件或設定檔教對方去動到那裡 ②本次上線會改變該風險的觸發條件 ③靜默型（出事沒有訊息、沒人會發現）。歸另案時須在信裡留下**項目計數**，不能只寫「另有一些」
- **刪掉信件範句裡的「跟這次交付無關」**：那句話寫在 Claude 會照抄進客戶信的模板裡，等於主動勸對方別看，而我方並不知道他的環境裡那項會不會出事。改為「不影響這次上線，但建議另外找時間處理」
- **新增降級揭露**：若有項目被歸另案、被壓成指路、或被判定前提失效改寫過，**在訊息之外（不寫進信裡）告知使用者一行**，讓他有覆核機會——把不可消除的主觀判斷交給人覆核，而不是用更多條文去消除
- 補一句說明分級額度與砍法第 3 階**不衝突**（兩者用同一判準挑同一批檔案，額度先算可展開幾份、第 3 階是額度仍不夠時再收）——原本兩處作用在同一塊文字上、方向看似相反

### Known
- 對抗審查已達 round_cap（5 輪）強制停止。經評估**可接受、不建議再改**的已知限制：①**形狀與範例的結構同構**——它們本來就是從範例歸納出來的，已移除「決定組合標籤」消除查表映射，殘餘同構是歸納法的正常產物 ②**550 這個數值的精確性**——它是意圖聲明而非可驗收門檻，實際約束來自行數等代理指標 ③**LLM 無法在寫作中精確計數**——屬模型能力限制，文件已提供可數的代理指標 ④**「會不會受損」判準的殘餘主觀性**——再收窄會回到「凡安全性一律進信」的過度修正，改以降級揭露交人覆核 ⑤**範例只覆蓋 8 種決定組合裡的 3 種**——已明文承認缺口並為最危險的組合寫專屬指引；補「沒有使用者背書」的新範例反而會降低範例區權威性 ⑥**「對方要拿去報預算的數字」這個三句例外的邊界偏寬**

## [0.3.4] - 2026-07-27
### Fixed
- **上限咬錯地方**（0.3.3 的總長上限實測 binding，但咬住的是正常需求而非要修的病）：紅方實測三封模擬信——「主體＋8 檔」照規則 7 展開子項的信 802 字，**每一個必寫項都壓到極限後仍 621 字**，而唯一能回到 550 的做法是把逐檔清單壓成一行一檔，那正是直接違反規則 7 與決定一「主體」的寫法欄。原因是砍的三階全針對「警示類同類項」，對「逐檔說明本身吃掉 57% 預算」完全無效。改為**基準 550 字、五份檔以內適用，每多一份檔加 40 字且該額度只能用在逐檔說明上**——上限要咬住規則堆疊出的贅句，不是咬住對方唯一想要的檔案清單
- 砍的順序新增第 3 階「**逐檔說明分級**」（只展開對方要拿去審/簽核/照著做的，其餘一行帶過），並明訂**這一步比砍警示優先**（警示砍掉是實害，檔案說明壓短只是少點方便）
- 補「最嚴重」與「同類項」的判準（原本兩詞皆無定義）：最嚴重照保留順位表、同順位比「出事後多久會發現」（越晚發現越嚴重）；同類項須「同順位且對方會採同一動作處理」才可合併（一項改設定＋一項要給資料不可合併）
- **新增跨類保留優先序表**（原本三套必寫清單共用一個上限，只有同類項內部壓縮規則、沒有跨清單取捨）：順位 1 對方不知道就會受損（絕不砍）→ 2 卡住對方下一步 → 3 分眾共通項 → 4 帶數字結論與逐檔說明 → 5 範圍邊界/負面結論/版本差異；並明訂順位 1、2 最多壓成「半句＋指路」，不會整項消失
- 修 **550 與 500 的自我矛盾**：0.3.3 補上限時漏改一處，「主體＋有提問」組合寫的是 500，同一個上限兩個數值
- 修「每題最多三句」與範例四的矛盾：範例四第 2 題實測 5 句，卻在兩處被指名為「每題最多三句」的示範。改為明訂三句是常態上限、遇前提校正或對方要拿去報預算的數字可放寬到五句，並在範例四標註該題是刻意的例外
- 修「四件事各自獨立決定、可自由組合」的錯誤宣稱（三輪修改已累積四處耦合：密度由檔案地位推導、規則 7 由決定一 gate、兩處版面由決定組合決定）。改為明訂**決定順序（地位→提問→受眾→密度）**並說明密度是前三者的結果，先定密度再回頭套地位會得到互斥取值
- 形狀 B 骨架補上「跨受眾共通項」的格位（規則要求它獨立一行放在開場後、第一個 ▌ 前，但骨架與範例三都沒有那一格，Claude 照骨架填時必然漏掉，共通事實會被塞進某一區而其他受眾漏看）
- 「主體＋有提問」組合補省法指引（該組合最容易超長，且明文禁止壓縮逐檔說明——補：省在逐題回答只留結論＋指路、檔案超過四份時只展開對方要用的）

## [0.3.3] - 2026-07-27
### Fixed
- **修掉 0.3.1 的過度修正（實測回歸）**：0.3.1 新增的八條規則全部單向往「多寫」推、且沒有任何反向制動。實際模擬一封八條全觸發的信實測 1,437 個中文字，而結構最接近的範例三只有 189 字（7.6 倍）、被標為「高壓縮」的範例四 539 字也被推到 898 字——等於把信推回舊版被連退三次的長度區間。修法不是撤回那八條（它們補的是真缺陷），而是補上缺的那一半
- 新增**總長上限 550 中文字（400 字就該檢查）**，並明訂「這條贏過所有『必寫』『一律進信』『要留』的規則」——所有必寫項在上限內競爭，不是各自加一段；超過時照「合併同類項→只留是什麼＋在哪章→只留最嚴重一項＋其餘合併指路」順序砍。上限值取自四個使用者滿意範例的實測（189/473/526/539），與範例自我一致
- 收窄兩個無上界的閘門：①「有疑慮時一律進信」→ 改為「進信但受總長上限管制，形式可只是半句＋指路」，並要求判準落在「說得出誰在什麼情況損失什麼」而非「我不確定」②「凡是安全性失效一律進信」→ 區分「本次交付會觸發或放大的」（進信）與「盤點順手看到、與本次交付無因果關係的」（歸另案建議、合併一句指路），並要求用白話講後果不用術語講手法（原條文與寫法規則 1 把 SQL injection 列為只留工程端那一檔直接矛盾）
- 收窄密度判準的「或對現況產生錯誤認知」：明訂「錯誤認知 ≠ 認知不完整」，要到「會據此形成具體而錯誤的結論」才算，擋掉補充說明性質的句子（原措辭不可證偽，邏輯終點是「所有真實資訊都必須寫」）
- 補「檔案主體＋有提問」的**排版**裁決（0.3.1 只裁了密度沒裁排版，導致逐檔清單放開頭與題目當骨架互斥）：題目骨架放前面、逐檔說明合併成結尾一區只寫一次、不拆進各題

## [0.3.2] - 2026-07-27
### Fixed
- **觸發詞補齊**：0.3.0 把「決定二」的問題來源放寬到會議待決事項、驗收檢核項、反覆追問的疑慮、反方向的阻塞型待回覆，但 description 的觸發語全部錨在「交付」二字，放寬的能力實際碰不到。補上「回覆會議留的待決事項」「答覆驗收檢核項」「對方追問的那個疑慮要回他」「寫信請對方回覆才能繼續」「這案子卡在對方還沒給資料」等觸發語
- 補英文觸發語句（原本只有 `deliver`／`handoff` 兩個裸單詞）：`write a handoff note`、`draft the delivery email`
- description 補上排除線索作為第一道篩：明訂前提「已有一批產出檔案」，並列出不該用的情境與「工作日報請用 daily-report」的消歧（原本只有正向清單，「交付」在軟體語境也指部署交付，且「交付報告給 PM 講 sprint 做了什麼」會正向命中但正文明文排除）
- 三處描述（SKILL.md frontmatter／plugin.json／marketplace.json）內容互不覆蓋，已同步；frontmatter 補上原本沒提的「七條寫法規則」與「四個範例」
- **README 修正**：plugin README 仍在教 `Hi <收件人>，`——正文鐵則①三處明文禁止的佔位符，且該規則在 0.1.1 就改掉了、README 落後四個版本。整份改寫為 0.3.x 的四決定結構。repo 層 README 的描述仍是「固定骨架順序」（0.3.0 的反面），一併更新
- **補回 0.3.0／0.3.1 的 CHANGELOG 條目**（版號已 bump 但變更紀錄沒跟，CHANGELOG 最新條目還停在 0.2.0，內容甚至與現行行為相反）
- 修匿名化造成的語意破損：「B 論壇論壇」（重複字）、「我方 這邊」「我方 認同」（替換遺留空格，且「我方」是公文用語，與「像同事交接不是公文」的語氣規則衝突）——範例四是唯一的高壓縮密度標竿，破損會被當語氣範本學走
- 刪除 plugin 目錄內兩個 `.bak`（內含匿名化前的真實人名與 DB 主機名；雖已被 .gitignore 排除不會外流，但會誤導維護者以為已廢除的選型表還在用）

## [0.3.1] - 2026-07-27
### Fixed
- 經對抗審查修掉兩項實害級缺陷：①誠實邊界仲裁表原以「會不會阻斷對方下一步」分流，漏掉「對方不必動作、沿用現狀就會受害」的潛伏型風險，且原示例把「登入憑證安全屬性靜默失效」判為不必進信——改為以「對方不知道會不會有人受損」分流，新增硬規則「凡安全性失效一律進信，不因是我方施作紀律而降級」＋「有疑慮時一律進信」②「前提校正」原只給客觀失效的例子、無防呆，會被讀成授權擅自改寫對方的問題（實測會寫出「先校正一下前提：這批資料不需要清理」而對方要的工時數字整封信沒出現）——補「先答再建議」正確寫法、三類不可校正情形、「拿不準時一律照答再建議」
- 密度判準由「會做錯決定嗎」擴為「會做錯事或對現況產生錯誤認知」，點名四類原本會被砍掉的必寫內容（潛伏型風險、阻塞型待回覆、負面結論、範圍邊界）
- 明禁拿決定組合去比對範例標籤挑最接近的照抄（8 種組合僅 3 種有範例，差一維照抄即是換皮的選型表）；改為「範例只抄語氣用詞、不抄結構」
- 補三處覆蓋缺口：分眾的跨受眾共通區（停用日這類所有人都要知道的事原無位置）、「主體＋有提問」的密度仲裁（原兩條規則結論相反且無範例）、決定一補「交付物本身就是答案」與「進度型交付」兩種歸類（含狀態標記慣例）
- 反方向的「阻塞型待回覆」（我方提問、對方不回就停工）改為獨立一區、放結論後、逐條編號並寫明回覆後時程，不再塞進「需別人配合確認」的附屬區
- 範例全面匿名化：移除真實客戶名、網域、內部 IP、DB 主機名與人名（原本會隨公開 repo 外流）

## [0.3.0] - 2026-07-27
### Changed
- **拆掉三型選型表**（簡潔型／展開敘事型／多受眾分流型），改為「動筆前先定四件事」：檔案是主體還是佐證、有沒有對方的問題要回、單一收件人還是多類分眾、內文密度；四者各自獨立、可自由組合，形狀自然長出來。原因：選型表要窮舉情境（窮舉不完），且逼模型先做分類決策，分類錯後面全錯
- 原三個骨架降級為「常見形狀參考」（形狀 A 逐檔導讀／B 分眾／C 逐題回覆），明示「是產物不是規範，組合跟哪個都不同就自己長」
- 新增「逐題回覆」形狀與第四個範例，補上舊版完全沒有的「按對方題號組織整封信」能力（含題目標籤照抄對方用詞、以最新來信原文核對題數、前提失效先校正再答）
- 語氣規則按面向重排為七條，第 7 條標明僅限「檔案為主體」時適用（原本兩條擴張性規則會被誤用到高壓縮的信上，是舊版寫太長的主因）
- 起因：舊版寫的交付信被連退三次——太長、細節太多、沿用已失效的提問框架

## [0.2.0] - 2026-07-06
### Changed
- 新增第三型「多受眾分流型」（▌ 按受眾切區塊：該讀哪幾份＋閱讀順序＋一行重點＋範圍邊界聲明）＋選型規則表（判準=收件人結構非檔案數）；語氣規則加「分流型術語分級按區塊不按檔」；收錄碳排模組信為範例三 few-shot 並擴充提煉要點（同檔可跨區塊、重點行=濃縮先修、邊界先講）

## [0.1.2] - 2026-07-03
### Fixed
- 加第二個展開敘事型範本(OppTrack)：開場回應委託、逐檔展開子項、結論改帶判斷的敘事(核心/風險最高優先/零流量可移除)、誠實講做不到；骨架改為可有機展開非填空格；新增硬規則：對外說明嚴禁任何內部術語與流程字眼(紅藍對抗/QA/code-review/adopt/bump/skill/plugin/session/AI…)

## [0.1.1] - 2026-07-03
### Fixed
- 收件人沒指定改輸出乾淨的 Hi，（不再留 <收件人> 佔位符，忘改會難看）；強化語氣規則：預設讀者不懂技術，術語只留給工程端那一檔、其餘白話，少用英文縮寫

## [0.1.0] - 2026-07-03
### Added
- 首版：交付訊息產生器。把本次 session 的交付產出寫成一段口語、務實、逐檔導讀的交付訊息，固定 fulin 慣用格式與語氣（Hi 稱呼→一句話交代這批是什麼→逐檔白話清單→背景範圍與帶數字的重點結論→有前版才寫的差異＋原因→導讀動線→輕鬆收尾）。
- 語氣規則明列：口語務實、技術詞翻人話（專有名詞只留在給工程端那一檔）、結論先講帶數字、變動要給原因不只報數字、輕鬆收尾。
- 收件人不預填人名（使用者要求）。
- 寫訊息前順手整理交付清單：列實際檔案、檢查版本一致性、依「彙總→明細→原始資料→實作/設定」邏輯排序，不擅自改檔。
- 內建 few-shot 範例（AWS WAF 交付），作為語氣與密度的對齊基準。

---

## 併入前的 daily-report 版本紀錄（獨立 plugin 時期，至 0.6.4）

併入時原樣保留，供追溯 Gmail OAuth／Workspace 帳號等踩雷紀錄。

## [0.6.4] - 2026-07-22
### Fixed
- README 補公司 Workspace 帳號現實：交付路徑表的 app password 標註「公司帳號多半不可用」，新增 Workspace 警告塊（管理員預設停用 app password、頁面顯示 setting not available、判斷方式、OAuth 在 Workspace 也可能被限制需 IT 放行）；補「client_id/secret 可直接給 Claude、桌面 app secret 非機密」提示；已知風險段的 app password 條補上個人 vs 公司帳號的差別

## [0.6.3] - 2026-07-22
### Fixed
- 把公司 Workspace 帳號的現實編進引導（實測踩雷：作者公司帳號 app password 頁顯示 setting not available）：① app_password 選項標籤與代價明標「公司 Workspace 帳號多半被管理員停用，別白試、直接走 OAuth」② app_password guide 新增步驟 0 帳號類型前置判斷，公司帳號看到 not available 就停 ③ 修正誤導措辭——桌面 app 的 client_id/secret 按 Google 設計就不是機密（隨程式散佈本來就藏不住），可直接請使用者貼給腳本，不必遮掩造成卡關錯覺；app password 才是真憑證仍不進對話

## [0.6.2] - 2026-07-22
### Fixed
- README 與程式對齊 0.6.0 收件人紀律：README 到處說「寄給設定檔指定的收件人」但實際收件人只認專案層、家目錄不當預設——修正 README 開頭與新增「憑證與收件人分兩層」段講清楚兩層設定；config.example 移除誤導的家目錄 recipients（程式已不採用），新增 daily-report.project.example.json 專案設定範本；doctor 收件人檢查同步改只認專案層（原本讀家目錄 recipients，會顯示借用來的收件人為綠燈誤導使用者）；doctor 檢查項 README 從 5 改 6（補寄件帳號）；setup 完成提示不再說「設定檔補 recipients」改說「各專案自設」

## [0.6.1] - 2026-07-21
### Fixed
- SKILL.md 呈現核可步驟明訂：寄送資訊每一項（尤其主旨）必須顯示 dry-run 印出的實際值，不准用「（日報前綴）」這類佔位符或示意文字——否則使用者核可的是示意版而非真正會寄的內容，呈現核可形同虛設。起因：呈現時用佔位符代替實際主旨給使用者看

## [0.6.0] - 2026-07-21
### Changed
- 收件人紀律修正（安全）：收件人只認專案層 .claude/daily-report.json，家目錄的 recipients 不再當預設——沒設收件人的專案改為拒寄並走引導，不再默默借用家目錄收件人寄給不相干的人（起因：測試時未設專案收件人的專案直接寄給家目錄的 aa22942072）；send_common.resolve_recipients 與 setup_gate.detect 同步此紀律。郵件排版拿掉手刻 HTML（卡片/強調條/inline style/置中 table 全砍），md_to_html 改輸出最基本的 p/ul/li/b，交給 Gmail 預設渲染——華麗 HTML 讓信不像個人寫的；移除連帶用不到的 build_meta 與樣式常數

## [0.5.1] - 2026-07-20
### Fixed
- 重構：抽出 send_common.py 統一兩條寄送路徑的前置契約（展開路徑/解析收件人/內容閘/確認窗口閘/sent 去重/scope_key）。原本 send_gmail 與 gmail_oauth 各自實作同一套檢查，紅隊已證實對等性靠複製貼上必然失衡（SMTP 曾漏兩道閘）；現在兩路徑跑同一份 prepare_send，各腳本只保留「怎麼把郵件送出去」（SMTP 連線 vs REST 呼叫）。scope_key/sent_path 單一事實來源移入 send_common，confirm_gate 改為 import 而非重實作，消除雜湊不一致導致去重漏接的風險。行為經 OAuth/SMTP 雙路徑實測一致（內容閘 exit 3、確認閘 exit 5、乾淨內容通過、arm→check→auto 全鏈路）

## [0.5.0] - 2026-07-20
### Changed
- 紅藍對抗（對外釋出視角，2 個獨立 fresh-context 紅隊）修 3 CRITICAL + 4 HIGH/MEDIUM：① SMTP 路徑補齊 --auto 確認閘與 sent 去重（原本只有 OAuth 路徑有，選 app_password 的使用者等於零核可保護、可無限重寄）② content_guard 擴充憑證/個資/金額偵測（原本只擋 AI 詞彙，含明文密碼與身分證字號的日報被判通過；密碼 pattern 要求值具密碼特徵以免誤判正常敘述）③ confirm_gate 狀態改以「日期+專案雜湊」為鍵（原本只用日期，多專案同日互相覆蓋/TOCTOU 比對錯檔/veto 連坐/誤判已寄——而多專案分設收件人正是主打功能）④ setup_gate channel 明示優先於憑證偵測（mcp_draft 選項原本永遠無法完成設定）⑤ 四支腳本補 expanduser（SKILL.md 用 ~ 開頭路徑，PowerShell/subprocess 不展開）⑥ arm 補建 reports/ 目錄 ⑦ README 修正與實況矛盾的「絕不自動寄」描述、補列 setup_gate/content_guard/confirm_gate、界線改為誠實說明語意層機密與私人 session 過濾的自律本質

## [0.4.0] - 2026-07-20
### Changed
- extract 預設只掃當前專案（--project/--all-projects，用 session cwd 判歸屬，輸出檔名帶專案識別）；收件人支援專案層覆寫（<專案>/.claude/daily-report.json 只能覆寫 recipients/cc/subject_prefix/from_name，憑證強制只從家目錄讀，已實測專案層假憑證被忽略）；gmail_oauth 新增 doctor 子命令：實打 API 逐項驗證（設定檔/用戶端/授權換 token/Gmail API 是否啟用/收件人），失敗輸出 Google 原始錯誤與連結而非寫死的 Console 路徑——起因是作者自己照引導跑仍漏掉啟用 Gmail API 而被 403；SKILL.md 與 guide 改為「doctor 判定、不信自我回報、Console 路徑以 Google 即時訊息為準」

## [0.3.0] - 2026-07-20
### Added
- setup_gate.py 機制閘：把首次設定從「模型自律講清楚」改成腳本產生內容。`status`（四種未完成情境統一判定，exit 10=需引導、0=可交付）、`options`（三選項的 label/description 由腳本輸出，權限/有效期/設定成本/代價四欄固定不可摘要，供模型原文填進 AskUserQuestion）、`guide <channel>`（逐步引導含每步「確認」判準）。
- check_no_secrets.py：掃 repo 內 JSON 範本有無真憑證（`--staged` 供 pre-commit）。
- .gitignore 擋 daily-report 真設定檔路徑。

### Changed
- SKILL.md 第 5 步改為「先跑 setup_gate status，退出碼決定走哪條」，禁止自行讀檔推論或直接詢問使用者；首次設定引導改為搬運腳本輸出，並明訂兩條紅線（憑證不進對話、範本不填真值）。

## [0.2.0] - 2026-07-20
### Added
- Gmail API OAuth 路徑（gmail_oauth.py）：`setup` 引導式授權（loopback redirect + PKCE S256 + state 防 CSRF，瀏覽器自動開、使用者按一下允許即完成，refresh_token 自動寫入 config）、`send`（refresh→access token 現換不落盤、Gmail API users.messages.send）、`status`（授權健檢）。零外部套件（urllib 直打 endpoint），scope 僅 gmail.send（能寄不能讀）。
- 交付管道三選一自動判斷（oauth → smtp → MCP 草稿），依使用者 config 內容決定，不重複詢問。
- SKILL.md 新增首次設定引導：Claude 逐步帶使用者開 GCP 專案→啟用 Gmail API→同意畫面→測試使用者→建桌面用戶端→執行授權，含「未驗證應用程式」警告畫面與測試模式 7 天過期的預先說明。

### Changed
- 刻意不內建任何 OAuth 憑證（選項 B）：避免配額/驗證狀態/撤銷風險綁在單一發布者，也避免個人憑證進公開 repo 的 git 歷史。

## [0.1.0] - 2026-07-20
### Added
- 初版：extract_sessions.py（掃 session jsonl → 按專案分組中間 JSON；mtime 預過濾、isSidechain/SDK/promptSource 系統注入過濾、ai-title 直取、prompt 截 400 字）＋ send_gmail.py（Gmail SMTP 應用程式密碼寄送，--dry-run 預覽、plain+HTML 雙格式、收件人設定檔管理）＋ SKILL.md 流程（萃取→生成→人工核可→寄送；統計僅本機顯示；私人 session 預設略過）
