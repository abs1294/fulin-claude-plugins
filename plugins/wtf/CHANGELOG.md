# Changelog

所有版本的變更紀錄。SKILL.md 每次調用都整份進 context，故變更紀錄放這裡不放 SKILL.md。

## 0.12.0 — 2026-09-14

**archify `workflow` 圖特化：六條硬規則讓線走直、泳道不虛高**。使用者比對兩張 archify 產出的 HTML 後指出結構差異，原話：「我喜歡 STRAIGHT 的線，不要繞 + 高度要拉低，沒特化的版本每一個泳道的高度都很怪」。

- **成因查到原始碼層**（`renderers/workflow/workflow-compiler.mjs:489-490`）：`baseContentH = max(74, ceil(maxVerticalExtent*2+8))`、`laneH = 30 + baseContentH`，其中 `maxVerticalExtent` 取**全圖**任一節點的 `|yOffset| + 節點高/2`，算出的 `laneH` **套用到每一條泳道**。所以任何一個節點設了 `yOffset`，全部泳道一起變高——節點少的那幾條就空一大片，這就是「每個泳道高度都很怪」。
- **本機反推驗證**：沒特化版泳道 298px → `baseContentH=268` → `maxVerticalExtent=130`；該圖最高節點 68px（半高 34）→ 反推 `|yOffset|=96`，算式完全吻合。⚠️ 過程中一度誤判成因是「有 256/257px 的超高節點」，實際查證發現那兩個是 `composition-frame-kind="group"` 的 group 框不是節點，該推論**已推翻**。
- **六條硬規則**（寫進 SKILL.md 的 archify 分支）：① 主線節點全放同一 lane、`col` 連號 0..5；② 主線每條 edge 三件套 `route:"straight"` ＋ `fromSide:"right"` ＋ `toSide:"left"`（只給 route 不夠，自動路由仍會繞）；③ **全圖禁用 `yOffset`**；④ `col` 上限 5；⑤ 角色用獨立 lane 以 `variant:"dashed"`／`role:"async"` 虛線連；⑥ 分岔節點與來源節點同 `col`。附可直接套用的 JSON 骨架與三道指令（validate／deliver／visual-check）。
- `route` 合法值記錄為 `auto｜straight｜drop｜outside-right｜return-left｜bottom-channel｜up-channel`，並註明**只有 `straight` 經實測**，其餘未驗。`meta.viewBox` 只吃兩個值且有硬下限。
- **實跑驗證**（archify 2.17.0-dev.1）：照骨架寫 JSON 實際跑完三道指令——`validate` exit 0 且 issues 空、`deliver` exit 0 產出 801KB HTML、`visual-check` exit 0 且 `readabilityOk:true`（真 Chrome 四張截圖）。量測結果：三條泳道**全部 104px**（＝archify 預設下限，無虛高）、三條主線 edge **全為 1 段直線**。對照沒特化版：泳道 298px、主線直線率僅 20%（3/15）。
- 資料來源：佈局參數與六條規則由另一 session（claude-0f）提供並附其 A/B 實測（同一份 JSON 只加 `yOffset:80`，viewBox 從 `1033×528` 變 `1033×966`，高 +83%、寬不變）；本 session 獨立以兩份 HTML 量測與原始碼反推交叉印證後才寫入。

## 0.11.0 — 2026-09-14

**寬度標尺改為機械產生（`ruler.js`），兩輪二分法**。使用者實測回報：「同一個電腦同一個螢幕，有的你量出來是 170，有的 200」，並裁決「量法我要機械的方式處理，我不要你用模型產出」。

- **新增 `skills/wtf/ruler.js`**：標尺不再由模型現場寫 print 迴圈。舊做法有兩個飄移源——① 模型每次現場寫，檔位與寫法會變；② 舊標尺 20 格一跳，真值落在區間中間時兩邊都「看起來沒折」，同一台機器可能答 160 也可能答 180。腳本三次執行 MD5 相同（實測 `42e12c16…`），逐字節無飄移。
- **兩輪二分法，精度 ±4 格**：第一輪 `node ruler.js`（80~220 每 20 格）問「哪一條開始斷開」找區間；第二輪 `node ruler.js <lo> <hi>`（每 4 格）問「最後一條沒斷行的是哪個」。邊界情況有出口：答「都沒斷」→ 細測 200 220，答「第一條就斷」→ 細測 40 80。
- **每行顯示寬度精確等於標示數字**（`<n-1-len(label)> 個橫線 + ┤ + 數字`），實測八條粗標尺與六條細標尺逐行驗證全部相符。因此**寫回 config.json 時直接寫使用者回報的數字，不再自行減安全邊際**——舊規則「取值時比他回報的數字略小一點」已移除，那是標尺不準時的補償，現在不需要。
- **`guard-terminal-width.js` 的 stderr 同步改寫**：它是模型被擋下時唯一讀得到的指示，原本印著舊的 print 迴圈。改成指向 `ruler.js` 絕對路徑（新增 `RULER` 常數）並寫明兩輪流程與「不准自己 print」。
- **修互鎖 bug**：`guard-no-execute.js`（0.10.0 新增）會擋掉 `node ruler.js`——寬度閘叫模型跑標尺、只執行閘不准它跑，兩閘互鎖。白名單加入 `/\bruler\.js["']?(\s|$)/`，涵蓋裸檔名、帶參數、引號包住的絕對路徑、unix 路徑四種形式；`ruler.js.evil` 這類偽裝檔名仍被擋。
- SKILL.md「問法」整節重寫（流程圖＋兩輪對照表），自檢第 1 題補兩問（「我是跑 ruler.js 還是自己 print」「有沒有跑第二輪」）。
- **驗證**：ruler.js 逐行寬度 14 條全對、三次輸出 MD5 一致、錯誤參數 exit 1；hook 實跑 exit 2 且路徑正確展開；回歸測試 10 項（含白名單、跨 session 隔離、三種 fail-open、寬度閘未破壞）全過。三檔均驗 CRLF=0。
- ⚠️ 過程紀錄：改 hook stderr 時第一次用 Python 非 raw string 寫入，`
` 被寫成真換行導致 JS 字串 literal 斷行、SyntaxError——正是既有教訓「用工具寫入字面的跳脫序列」。已從備份還原後改用 raw string 重做。

## 0.10.0 — 2026-09-14

**第零鐵則：只解釋，不執行**。使用者實際遭遇：按了 `/wtf` 之後模型仍去改了檔案，他的原話是「我用 wtf 是讓你解釋耶，不是要你改耶」「他不要再去執行了他是要解釋，今天就算是我傳一個動詞過去，他也要解釋」。

根因是本檔自己寫的規則。舊版「特別注意這幾種情況」第一條明文寫著**「先照他原本的話做，再解釋」**——模型讀到那條就會動手。而 frontmatter 的 `disable-model-invocation: true` 只擋「模型自主啟動 wtf」，完全沒管「使用者啟動 wtf 之後，模型怎麼對待他接下來說的話」。兩者合起來就是這次的洞。

- **新增第零鐵則（凌駕本檔所有其他規則）**：使用者在 `/wtf` 之後說的每一句話都是「要被解釋的材料」，不是「要被執行的指令」。附判斷流程圖與六列對照表，明確涵蓋三種型態——**祈使句**（「改那個檔」→ 講那是什麼、改了會影響什麼）、**單一動詞**（「改」「跑」「修」→ 講清楚有哪幾種可能意思再問）、**罵人的話**（「你這個白癡」→ 他在表達沒看懂或不滿，不是在下指令，不准慌了就動手補償）。
- **兩條常見藉口寫死禁止**：「他語氣很急、應該是真的要我做」（語氣急代表他更需要看懂）、「這件事很小、做了比較快」（他按 /wtf 就是還沒搞清楚狀況，這種時候動手最危險）。
- **刪掉矛盾規則**：「先照他原本的話做，再解釋」改為「當場把落差講清楚（做了什麼／差在哪／為什麼差／照做會動到什麼），然後問要不要現在動手」，並加一句「不要因為心虛就直接動手補救」。
- **新增 PreToolUse 機械閘 `hooks/guard-no-execute.js`**。使用者裁決要文字鐵則＋機械閘兩層，理由是既有教訓「skill 寫『必須』是自律、AI 會繞；只有 hook 是他律」。做法：看到 `Skill`＝wtf 就在 `os.tmpdir()` 立旗（沿用 cc-statusline `skill-tracker.js` 的 session_id 命名法），旗在期間 Edit／Write／NotebookEdit／MultiEdit／Bash／PowerShell 一律 exit 2 擋下，stderr 告訴模型該改為解釋。**30 分鐘 TTL 自動失效**，避免一次 /wtf 鎖住整個 session。全程 fail-open（空 stdin／壞 JSON／null payload／讀不到旗一律放行）。
- **白名單放行 wtf 自身動作**（使用者裁決：寬度設定與畫圖是 wtf 的本體功能，不是在執行使用者的指令）：寫 `skills/wtf/config.json`、寫 OS 暫存目錄的 `.html` 圖表、開瀏覽器指令（`start ""`／`Start-Process`／`open`／`xdg-open`）、`archify.mjs doctor|guide|validate|deliver`。路徑比對用 `path.relative` 防目錄跳脫。
- `hooks.json` 註冊兩個 matcher：`Skill`（兩支 hook 並存，寬度閘不受影響）與 `Edit|Write|NotebookEdit|MultiEdit|Bash|PowerShell`。
- frontmatter description 與 `plugin.json` description 同步寫入本鐵則——模型載入時第一眼就看到，不必讀到正文。
- 自檢十八題 → **十九題**，新增第 0 題「我這一輪有沒有動手改東西、跑指令、裝東西？」置於清單最前。
- **驗證**：13 項實跑（非語法檢查）全過——沒按 wtf 時放行、按了之後擋 Edit、擋 `rm -rf`、四種白名單放行、跨 session 不污染、三種 fail-open、寬度閘未被破壞、非 wtf skill 不立旗、過期旗自動清除、hooks.json 兩支 hook 檔案皆存在。SKILL.md 與 plugin.json 均驗過 CRLF=0、YAML frontmatter 三鍵完整。

## 0.9.0 — 2026-09-13

**HTML 升級分支改為優先調用 archify**（tt-a1i/archify，MIT）。使用者比較 wtf 與 archify 後裁決：兩者不是同類，wtf 是溝通紀律、archify 是渲染引擎，唯一重疊處是 wtf 的「ASCII 排不出來 → 升級 HTML」那條——原本叫模型手刻 HTML，等於寫的人跟驗的人是同一個，沒有外部檢查擋得住排爛。

- **偵測跑 CLI 驗活，不看目錄**：依序試 `~/.claude/skills/archify`、`~/.agents/skills/archify`，跑 `node <路徑>/bin/archify.mjs doctor`，**exit 0 才算可用**。實測依據：本機 `~/.claude/skills/archify` 是 symlink 指向 `~/.agents/skills/archify`，`test -d` 會過但不代表跑得動（Node 版本不符、檔案缺一半，目錄照樣存在）。兩條路徑實跑 doctor 皆 exit 0，不存在路徑 exit 1。
- **沒裝就問，不准自己裝**：安裝會寫檔到家目錄、會對外連線抓套件。偵測不到就問「要不要我幫你裝 archify？」，他明確說要才動手；說不要或沒回應一律回退手刻，不卡著等他（他按 /wtf 是要看懂東西，不是要處理安裝）。官方指令 `npx skills add tt-a1i/archify -g`，不自創 clone 寫法；裝完再驗活一次才走 archify。
- **圖型對照**：wtf 三個升級理由對得上 archify 五型——要 mermaid 的時序圖→`sequence`、狀態機→`lifecycle`、太密的流程→`workflow`；另有 `architecture`、`dataflow`。手上已有 mermaid 直接餵給它轉。
- **`deliver` exit 非 0 就是沒產出，不准說成功**。實測：正常輸入 exit 0、9/9 checks pass、產出 814102 bytes HTML；壞輸入 exit 1 且 HTML 完全不產出。
- **archify 的回報口吻不進對話**：它要求回報 validation summary、receipt、SHA-256、browser-evidence status，那是工程交付格式，跟本 skill 的白話重講相反——使用者按 /wtf 是看不懂，丟一串雜湊值只會更看不懂。對話裡只留「已開在瀏覽器，講的是 X」。
- **archify 有連外的版本檢查**（`scripts/check-update.mjs`）：報有新版時給一則精簡通知就好，不准自己更新。
- 原有五條 HTML 規則全部保留，移到「回退手刻」分支：放 OS 暫存目錄、排他建立、開給他看、開檔失敗不准假裝開了、終端機仍留一句結論。
- 自檢第 14 題擴充為五問（驗活跑了沒／有沒有自己裝或靜默跳過／deliver exit／暫存目錄與排他建立／有沒有把 SHA-256 丟進對話）。
- **問使用者要不要裝時，不准只丟名字**。使用者裁決：「你問人家要不要裝的時候要說明一下這是啥」——對一個正在說「我看不懂」的人問「要不要裝 archify？」，等於要他為一個陌生名詞做決定，違反本 skill 自己的「非用不可的新詞第一次出現就要當場白話解釋」。規則改成四件事一次講完（這是什麼／為什麼要它／裝了會動到什麼／不裝會怎樣），附可改寫的範本；問句放回覆末尾不放開頭（先把圖給他，安裝的事附在後面），同一對話問過一次就不再問。自檢第 14 題補兩問。
- 偵測段的 symlink 說明從推測改為實證：`npx skills add` 真身放 `~/.agents/skills/<名稱>/`、`~/.claude/skills/` 掛 symlink，安裝紀錄在 `~/.agents/.skill-lock.json`（本機實測該檔記 `"source": "tt-a1i/archify"`、`"sourceType": "github"`、資料夾雜湊）；真身底下**沒有 `.git` 也沒有 `node_modules`**，是解壓後的純檔案，所以不能用「有沒有 .git」判斷裝了沒。
- 致謝區記下 archify 的「三種宣稱嚴格分離」（確定性檢查／瀏覽器證據／真人感知審查，通過其一不蘊含其二），對應本 skill 的「開檔失敗不准假裝開了」。

## 0.8.2 — 2026-09-07

**同構區塊合成一張大表**。使用者拿一張實戰截圖（四個 bug 各一塊散文、2×2 並排，每塊都是「在哪／怎麼發生／後果／修法」）說「像這種一個區塊一個區塊，應該畫大表格給我，閱讀體驗比較好」。原規則只分「同一件事→並排」「不相干→上下排」，漏了第三種：不相干的主題但**結構相同**——那是一張表的幾列，讀者要橫向對照「④的修法跟①差在哪」，散文對不起來。
- 判準表加一列「幾個主題、每個都講同樣幾個面向 → 一張大表」；區塊數流程圖的「不是」分支下再問一次「每塊都講同樣幾個面向嗎」。
- python 骨架加 `wrap()`（依顯示寬度折行，長詞先找 `./:_-` 斷）與 `table()`（列＝主題、欄＝面向，儲存格自動折行、box-drawing 框線）；用截圖那四個主題實跑，5 欄 168/170 格。
- 自檢 17 題 → 18 題（新增「有幾個結構相同的區塊」）。

## 0.8.1 — 2026-09-07

HTML 升級路線的檔案改放**作業系統暫存目錄**（Windows Bash `$TEMP`／PowerShell `$env:TEMP`、macOS／Linux `${TMPDIR:-/tmp}`），不再放專案暫存目錄；檔名 `wtf-<主題>-<yyyyMMdd-HHmmss>.html` 帶時間戳可回頭找，以排他建立防覆蓋（只有檔案已存在才換序號重試，其他錯誤停下回報）。開檔指令補 PowerShell `Start-Process -ErrorAction Stop`，失敗處理涵蓋 PowerShell 拋錯。使用者顧慮：「HTML 檔畢竟只是解釋給我的，我擔心太常用就一堆垃圾」——專案目錄本 skill 不清、只會累積（實測 Claude 的專案暫存 22 GB、1533 個 session 資料夾），OS 暫存目錄清不清取決於使用者系統設定，本 skill 不承諾保留期限（本機 Storage Sense 已開、`%TEMP%` 4842 項只有 3 項超過 30 天，那是這台的設定不是通則）。我第一版改成固定單檔覆蓋，使用者否決：要能 recall，時間戳與主題要留。自檢第 13 題補「是不是放在 OS 暫存目錄、用排他建立」。

## 0.8.0 — 2026-09-07

整合 humanlayer/skills 的 `show-me`（MIT，3.3KB 純範例、無硬規則）。使用者裁決：「show-me 可以讓我更看懂」——我原本把 mermaid 與 HTML 判「不抄」，理由是「wtf 是終端機場景」，那是我替 skill 設的限制，不是使用者的需求；使用者要的是看懂，不是留在終端機。

- **判準表補四列**：呼叫樹（縮排代表被誰呼叫，箭頭圖三層就爆寬）、偽代碼（縮排的中文句子，無語法無型別，**不算程式碼**——與「能用圖講就不貼 code」的邊界明寫）、diff 形式（`+`/`-` 講改了什麼，整段都新才貼整段）、檔案樹＋一行職責註解。
- **新增「終端機塞不下就升級成 HTML」一節**：門檻寫死三種（主題是視覺佈局／太密 170 格塞不進／需要 mermaid），「排 ASCII 很麻煩」不是理由。開檔指令三平台各寫（Windows `start`、macOS `open`、Linux `xdg-open`；show-me 原版只有 `open`，Windows 不通）；mermaid 只出現在這層，終端機不渲染。升級後終端機仍要留一句結論。
- 原版不抄的：`Bash(open …)` 單一平台寫法；「HTML 支援桌機與手機」（wtf 場景是本機瀏覽器）。原版一句話保留進文件：「挑能把重點講清楚的最小視圖，可以用一種或幾種，不會全部用上」。
- 送審後補三處（Codex BLOCK、code-reviewer 同時抓到其一）：①**判準表補「元件樹」**——原版 show-me 有 component tree（UI 結構＋state 邊界），我先前說「併入呼叫樹」是錯的：呼叫樹畫「誰呼叫誰」、元件樹畫「誰包誰」，兩件事。②**「太密」升級門檻綁死 `check()`**：必須先實跑 `check()` 失敗、拆成兩張並排仍失敗才算數，沒跑就說太密＝憑感覺不准——原本這節放棄了判準表其他規則都有的機械閘。③**開檔失敗的 fallback**：SSH／無 GUI 下 `start`/`open` 會失敗，exit 非 0 就改說「檔在此路徑、無法自動開」，絕不能開失敗仍宣稱「已開在瀏覽器」。另修 mermaid 措辭：原寫「只出現在 HTML 層」是我自加的綁定，原版 mermaid 是獨立 code block；改成「終端機看到的是原始碼不是圖（Claude Code 只在 Artifact 渲染），所以要圖就進 HTML」——結論同、理由講對。
- 複審再補（code-reviewer 抓到、Codex 沒抓——它只對照原版沒回頭看表內既有列）：新增的「元件樹」列與既有「包含關係、層級→縮排樹」列同時命中 UI 元件情境，違反判準表自己「這些項目不會同時成立」的要求。修法：縮排樹列縮窄為「目錄、組織、分類——不含 UI 元件」，元件樹列明寫「UI 元件一律用這列」。另 fallback 段「結論寫滿」補一句「寫滿＝事實不因降級而漏，不是多寫廢話」，防與密度規則誤讀。
- 自檢十六題 → 十七題（新增「有沒有把 ASCII 排得出來的圖丟成 HTML」）。frontmatter／plugin.json／marketplace 三處描述同步。

## 0.7.1 — 2026-09-06

- **更名 `what-the-fuck` → `wtf`**（使用者裁決：太長不好打）。plugin 目錄、skill 目錄、`plugin.json` name、`marketplace.json` name/source、SKILL.md frontmatter 一併改；hook 的 regex 改為 `/(^|:)(wtf|what-the-fuck)$/` **兩個名字都認**，打全名仍可用；`CONFIG` 路徑跟著 skill 目錄改。config.json 隨目錄 rename，發布值不變（null／false）。
- 相容殘留一併清掉（使用者裁決）：hook regex 改回單名 `/(^|:)wtf$/`，plugin.json description／keywords 與 SKILL.md frontmatter 不再提舊名。舊名 `/what-the-fuck` 不再存在，打了會找不到。
- ⚠️ 已裝舊名的機器：marketplace update 後會看到新 plugin `wtf`，舊的 `what-the-fuck` 條目消失；需 `/plugin install wtf@fulin-plugins`，舊 cache 目錄留著不影響。已填過的 terminalWidth 在舊目錄裡，新目錄要重問一次（同「更新後重問」機制）。

## 0.7.0 — 2026-09-06

實戰回饋：使用者指出「為什麼沒有按照 170 設計，右邊一堆空白」。當次回覆把三個區塊上下堆疊，170 格的視窗只用了 45 格。三處缺口都在「知道規則但排版時沒想到」，故一律改成機械判定。

- **`side()` 骨架旁補 `check(lines, term)` 雙向門檻**，並把「排完務必量一次最寬行」從一句提醒改成必跑步驟。原本只擋「超過視窗寬」單邊，漏掉的正是實際犯的那邊：`widest < term * 0.7` 代表區塊該並排卻上下堆疊。**下限這條目測抓不到**——畫的人眼裡每個區塊都是滿的，只有量過才知道整體只用了三分之一。已紅綠實測：並排後的實際版面 120/170 PASS、當次排爛的版本 45/170 FAIL、邊界 119 過 118 擋、超寬 171 擋。
- **新增「動筆畫圖之前先數區塊」流程圖**，並補上原本沒有的判準：兩個以上區塊時，還要分「同一件事的不同面向」（流程／差異／為什麼講的都是同一個東西 → 並排，讀者橫向掃一次看完）與「不相干的主題」（→ 上下排，硬並排反而難讀）。舊版只寫「兩個以上的圖或表就並排」，沒區分這兩種情況；`side()` 也只出現在說明段落附近，讀起來像參考程式碼而非必跑步驟。
- **自檢第 11 題從目測改成跑 `check()`**：原文「右邊有沒有一大塊空白？」是主觀判斷，當次自檢過了卻仍然排爛。改為「我跑過 `check()` 了嗎？」並附門檻數字。
- 未修（使用者裁決不做）：「只有本次對話回答的數字才能寫回 config」。當次確實從 memory 撈到 170 直接寫入而未詢問，屬繞過規則，但使用者認為 170 正確、不需要加這條限制。

## 0.6.1 — 2026-09-06

紅藍對抗（實作模式，4 輪收斂）。紅方專攻十輪靜態審查的盲區：真的跑、異機異人異 shell。抓到 5 個 ≥MEDIUM 真弱點全修，全部附實跑重現與修後複驗。

- **hook 對 `null`／陣列／字串 payload 拋 TypeError（exit 1）**：官方語義「非 0/2 退出碼不阻擋」＝閘靜默失效。`try/catch` 原本只包 `JSON.parse`，後續 `input.tool_input` 沒防。已把型別檢查做滿（不是物件、`tool_input` 不是物件、`skill` 不是字串一律 exit 0 放行），8 種異常 payload 實測全放行、11 項正常矩陣不受影響。
- **config.json 壞掉時 stderr 說「terminalWidth = null」誤導模型重問**：尾逗號／截斷／空檔／BOM／檔案不存在五形態都落 catch 當成「未設定」，模型會以為沒問過而重問，問完寫回壞檔還是壞的→無限迴圈。已加 `configBroken` 分支：壞檔時早退、只印 6 行（不計空行）「這不是沒問過，是檔壞了，先重建再重新調用」，不印標尺雜訊；正常 null 路徑一字未動。
- **symlink 開發模式沒有 hook 但文件沒說**：把 skill symlink 進 `.claude/skills/` 只暴露 SKILL.md，plugin 的 hooks.json 不會註冊、閘不存在。SKILL.md 補一段警告；frontmatter 原本無條件寫「有 hook 機械閘擋著」改為「以 plugin 安裝時有…；symlink 模式沒有」。
- **E2E 七步實跑通過**（十輪靜態審查未做過）：發布 null→擋、寫回 170→放行、拒答設 declined→放行、拒答後主動補 200→放行。用 `CLAUDE_PLUGIN_ROOT` 指向副本、官方完整 payload 格式跑 hooks.json 的 command 原文。
- **hook 不查 `tool_name`，任何工具的 `tool_input` 恰好有 `skill` 欄位就被誤擋**（Codex 審查抓到，實測 `tool_name=NotSkill` 中招；v0.6.0 既有設計缺陷，紅藍對抗時我誤判為 LOW）。`matcher: "Skill"` 是 regex，未來以 Skill 開頭命名的其他工具也會觸發本 hook。修法：官方 payload 必帶 `tool_name`，帶了且不是 `Skill` 就放行；沒帶（手動測試）沿用只看 `skill`。實測三態：NotSkill 放行、Skill 仍擋、無 tool_name 相容。
- catch 分流補 `EACCES`／`EPERM` → 「沒有讀取權限」（原本混進「不是合法 JSON」支，code-reviewer 抓到）。`EISDIR` 落 JSON 支是 `readFileSync` 對目錄的必然結果，不誤導修復方向。
- 假陽性 3 個（stdin 永不 EOF、env 未設、冷啟動超時）經藍驗以本機 10 個生產 hook 同法運作／官方保證注入／實測 110ms 駁回。LOW 5 個只記錄不修：陣列 skill 名（型別檢查已順帶消除）、stderr 印含帳號名路徑、前導空白／大小寫 skill 名放行、多 session 同時寫 config 競態（後寫者贏且語意不衝突）、matcher `Skill` 為 regex 可能匹配未來以 Skill 開頭命名的工具（型別檢查已兜底）。
- 使用者本機 cache 仍是 0.5.0（無 hook），0.6.x 尚未 update——不是缺陷，但「已推、可以用」對使用者當下不成立。

## 0.6.0 — 2026-09-05

- 新增 **PreToolUse hook 機械閘**（`hooks/guard-terminal-width.js`，matcher 為 Skill）：調用本 skill 時若 `config.json` 的 `terminalWidth` 不是正整數，直接 exit 2 擋下本次調用，並把「印階梯標尺→問使用者→寫回設定檔」的完整指令從 stderr 塞回給模型。**為什麼要機械閘**：規則寫在 SKILL.md 裡靠模型自覺遵守，實測會漏——上一版才把讀設定檔寫成硬性第一步，下一個 session 仍然沒讀檔就直接問使用者，而使用者早就答過了。只認 skill 名為 `what-the-fuck`（含帶 namespace 的寫法），其他 skill 一律放行，裝了本 plugin 但不用這個 skill 的人不受影響；無 stdin 或 stdin 非 JSON 時放行，閘壞掉不卡使用者。已實跑十項紅綠測（擋：null／0／負數／字串型別／設定檔不存在／帶 namespace；放行：另外三個 skill／無 stdin／非 JSON／寬度 170）。
- **修一個機械閘造成的死結**（送審時被 code-reviewer 判 Critical）：hook 是 PreToolUse，擋在 SKILL.md 載入之前——使用者若明確說「不要問了」，SKILL.md 裡「那就停、照 80 排」那條降級規則模型永遠讀不到，因為每次重新調用都被同一個 exit 2 擋住。**閘擋住了它自己的逃生門**，而 v0.5.0（純文字規則）沒有這個洞。修法：config.json 新增 `widthPromptDeclined`，設為 true 時 hook 放行；stderr 也明講這條出路，並註明「必須是使用者明確拒絕過才准設，不准因為問了很煩就自己設」。已實跑七項驗證（null+無旗標擋／null+true 放行／null+false 擋／有寬度放行／字串 "true"、數字 1、"yes" 皆不得放行）。
- 複審再補：SKILL.md 的判斷樹原本只畫「有數字／是 null」兩支，拒答並設了 `widthPromptDeclined` 的使用者會落進「是 null → 強制先問」，與降級規則矛盾。已重畫成三支（有數字／null 且未拒答／null 且已拒答），第三支直接接到「照 80 排、只用一行輕量提醒」。
- **已知取捨（使用者裁決 A，刻意不修）**：`widthPromptDeclined` 該不該設，程式面無法驗證「使用者是否真的拒絕過」——唯一守門是 SKILL.md 那句純文字禁令，也就是說這個新出口的最後一道鎖仍是模型自覺，正是本次要修的病根搬了個位置（Codex 審查指出）。考慮過「每個新 session 自動重置成 false」逼一次性重問，經使用者裁決不採用：**濫設的後果只是圖表變窄（80 寬）不會爆版，代價遠小於讓使用者每開新 session 都被問一次**。
- 複審第三輪再補：判斷樹沒畫「有數字 + 已拒答」這第四種組合。補說明時我第一版寫錯成「declined 一律優先、有數字也走第三支（照 80 排）」——**那是把 hook 的職責誤讀成排版規則**。hook 只決定「擋不擋調用」，兩種情況都放行；「用多寬排版」是 SKILL.md 的事。正確語意：兩個欄位各管各的，`widthPromptDeclined` 管「還要不要再問」、`terminalWidth` 管「排版用多寬」，所以有數字時照數字排，他拒絕的是被問、不是拒絕自己給過的值。
- 複審第四輪：上一輪我只改了 SKILL.md 被點名的那一句，**同一句錯誤說法在另外三處還留著**——config.json 的 `_widthPromptDeclined` 註解（code-reviewer 抓到）、hook 的程式碼註解與 stderr 訊息（兩軌都沒抓到，我自己掃全檔才找出來）。已全部改成「這個旗標只管還要不要再問、不管排版；terminalWidth 有數字就照數字排，只有 null 時才退回 80」，並用判準「有沒有暗示 declined=true 就一律 80」複掃全 plugin 確認無第四處。**教訓：被指出一處就只改那一處，是這個 repo 的全域紀律早就禁止的（被點名一處＝掃全文），我照樣犯了；而且掃全文是我自己做的，兩軌都沒抓到 hook 那兩處。**
- 複審第五輪，Codex 指出一個更根本的問題：**判斷樹只有三支，第四種組合（有數字＋已拒答）我是用圖底下的散文補的**——而這個 skill 第 73 行自己明訂「fallback 鏈與擇一分支要用流程圖不是清單」，等於自我違反：本該畫進圖的路徑被降級成文字。它同時指出圖上沒畫「已拒答之後使用者主動補數字該怎麼接」這條回饋線。已把樹重畫成完整四支並補上回饋線，圖下方那段與圖重複的文字補丁一併刪掉（留著就是同一件事講兩次，違反本 skill 自己的密度規則）。四支行為已用 hook 實跑逐一驗證：有數字+未拒答／有數字+已拒答／null+已拒答 三支 exit 0，只有 null+未拒答 exit 2。**教訓：用文字補丁繞過「該畫圖」的要求，比漏畫更難發現——因為看起來已經講清楚了，只有拿 skill 自己的鐵則回頭檢驗才抓得到。**
- 複審第六輪，Codex 判 BLOCK：四支樹的匯流線接錯了。**兩軌對同一條線給出相反結論**（code-reviewer 說畫對、Codex 說連錯），兩邊都宣稱用程式算過座標，所以我自己按東亞寬度逐字元量測仲裁——Codex 對：163-164 行四支垂直線在 col 14／33／64／83，但 169 行在 col 32 憑空冒出一條線接到「他主動給了數字」，視覺上緊貼第一、二支下方，讀者會以為「已經有數字的人還要繞經『給數字』這個動作」，邏輯不通。第一、二支本來就該是死路。已重畫：一二支明確標「（終點）」不再往下拉線，只留第三支（強制先問→印標尺→答了標尺）與第四支（事後改變心意主動給數字）兩條線匯入「寫回 config.json」。重畫後逐項驗證：col 32 已消失、第四支線 163→177 行完整無斷、箭頭與轉角接點正確、圍欄配對 20 個偶數。**教訓：兩軌結論相反時不能挑一邊信，要自己拿可重現的量測仲裁——這次若信了 code-reviewer 就會把錯的圖推上去。**
- 複審第七輪，Codex 指出一個文字覆蓋率缺口：「使用者事後改變心意」只有第四支（null+已拒答）寫了，**第一支（有數字+未拒答，也就是最常見的長期路徑）從頭到尾沒提「他哪天主動說要改寬度該怎麼接」**。不是「（終點）」兩字會誤導，是這個情境根本沒被寫進規則。已補一句涵蓋四支：「圖上標『（終點）』只是說這一支不需要再問他，不是說寬度從此不能改——他任何時候主動說要改寬度，一律照做並寫回 config.json」。教訓：**圖畫對了不等於規則寫全了**，圖只表達「系統怎麼判」，沒表達「使用者主動要求時怎麼辦」。
- 補一個兩軌都沒回答的問題：使用者主動要求改寬度時，`widthPromptDeclined` 要不要一併改回 false？**實測後判定不用**——hook 對「有數字+declined=true」是放行（exit 0），排版看 `terminalWidth`，而 `declined` 只擋「主動詢問」、已經有數字時本來就不會問；硬改回 false 反而可能讓他之後又被問。已把這個判斷寫進 SKILL.md，避免以後有人在這裡猶豫。
- **修一句實測推翻的事實錯誤**：SKILL.md 與 config.json 都寫著「plugin 更新可能把 terminalWidth 蓋回 null」——**錯的**。2026-09-05 實測本機 cache：`~/.claude/plugins/cache/fulin-plugins/what-the-fuck/` 底下同時存在 `0.1.0/`、`0.4.0/`、`0.5.0/` 三個獨立資料夾，使用者填在 0.5.0 的值（170）完好無損、從未被覆寫，而遠端推的仍是 null。真相是「新版是全新資料夾、config.json 就是發布時的空檔」，**不是舊值被蓋掉**。結果一樣（更新後會重問一次），但原因不同——留著錯誤說法會讓模型以為「值被覆寫了」而去別處撈舊值。兩處已改成正確敘述並附實測依據。這個錯誤是 Codex 在複審時標為 HYPOTHESIS（它無法在自己環境實測 plugin 更新機制）才促使我去實測的。
- 前一條的敘述再加保留語（Codex 複審時挖到的）：它查到舊版目錄帶 `.orphaned_at` 與 `.in_use/<pid>` 兩個管理檔，證實「按版本號分資料夾」是 Claude Code 的正式版本管理機制而非巧合（用 deliver-report 的 11 個歷史版本等交叉驗證）；但也發現孤立版本已標記約 1-2 週卻還沒被清除，**暗示存在未來可能觸發的清理機制**。我實測確認 `.orphaned_at` 標記確實存在（cc-statusline、daily-report、deliver-report 等多個 plugin 皆有）。已加一句「別依賴『舊值永遠撈得回來』，該問就問」——即使未來真有清理機制，操作結論不變，但敘述不該寫死成永久保證。
- 收尾兩處（Codex 第十輪建議，皆採納）：①把「別依賴舊值永遠撈得回來、該問就問」從舉證括號內搬到括號外，與既有的「永遠不要因為應該問過了就跳過讀檔」並列——那是行為規則不是佐證，塞在舉證括號裡會被當次要補充略讀；括號內只留實測細節。②**config.json 的 `_note` 刻意不同步這句保留語**：SKILL.md 是規則正本且每次調用整份進 context，模型執行到「讀 config.json」這步時 SKILL.md 必然已在 context 裡，`_note` 只是操作提示的複誦、不需要全文同步。此處明記，避免日後被誤會成漏改。
- hook 的說明**刻意不寫進 SKILL.md**：被擋時 stderr 已把指令塞給模型，沒被擋時那段就是廢話——SKILL.md 每次調用整份進 context，不放不必要的內容。

## 0.5.0 — 2026-09-05

- **改用與 SKILL.md 同層的 `config.json` 存寬度**（使用者裁決）。前一版寫「問到後記進 memory」有兩個問題：memory 靠模型自覺去查會漏、且綁專案換專案就失效；中途我一度提議放 `~/.claude/`，被否決——**plugin 是要發布給別人的，設定檔必須跟著 plugin 走，不能依賴外部狀態**。定案：`config.json` 隨 plugin 發布、`terminalWidth` 預設 `null`，每位使用者在自己機器上填自己的值；skill 第一步硬性讀檔，讀到 null 就強制問、拿到就寫回。plugin 更新可能把值蓋回 null，那不是錯誤、重問一次即可。**教訓：我一度卡在「會被更新覆蓋」而不敢做，但覆蓋的後果只是「再問一次」，不是災難——把小成本誤判成阻礙，就會繞遠路去做更糟的設計（把個人設定打包進發布物，正是這輪在修的缺陷）。**

## 0.4.0 — 2026-09-05

- **移除硬編的 170**（使用者指出的真缺陷）：170 是「這一位使用者的視窗寬度」，不是通用值——skill 發布給別人用時，窄視窗的人會每張圖都爛版、寬視窗的人右邊浪費一大塊，而原文還明文寫著「不要自己重測」。改為**向使用者問到為止、不設 fallback**（使用者裁決：不要保守值，猜窄了浪費版面、猜寬了整頁爛版，兩者都不能接受）：先看本 session 問過沒 → 再找 memory → 都沒有就印階梯標尺問，答案寫進 memory 供後續 session 用；
- 新增「**框字不是畫圖**」一節（使用者連續指出三次同一個病）。判準：**把框線全部刪掉、只留文字，意思有沒有變？沒變就不是圖，是加了裝飾的清單。** 附反例對照（同一組內容用框字 vs 用真流程圖）與「框線要承載資訊的五種方式」表（線代表流向／線代表分岔／位置代表層級／欄位對齊代表對應／長度代表量），以上皆非就不要加框、直接條列更好讀。同時補判準表三列（**fallback 鏈、if-else 擇一、錯誤處理分支一律用流程圖**）與誤判警告（`①②③` 看起來像表格，但「①不成立才走②」是排他關係＝流程圖）。自檢擴為十六題，新增第 9、10 題就是刪框線測試與「這些項目會同時成立嗎」。**教訓：我用「有沒有畫圖」自檢，但盲區正是「什麼才算圖」——用有盲區的尺去量，量不出盲區本身；當時我還掃過七張圖宣稱「只錯一處」，那個掃描本身就是用同一把壞尺做的。****教訓：把「查證過程的結論」（程式量不到、中文佔兩格、手排必歪）和「這位使用者的環境值」（170）混在同一段寫，就會把個人設定當成通用規則發布出去。**

## 0.3.0 — 2026-09-05

- 併入 jackle.pro `/explain-visual` 的三個概念（使用者提供、經查證原文：作者的觸發時機「只要看到 AI 回覆有點看不下去」與本 skill 完全相同，且他同樣做成手動指令而非 output style，理由是「沒有每次回應都要視覺化」）：①**六個固定章節改成可選的深度階梯**——原版固定六章會讓重講版變長，與本 skill「要更短」直接衝突，改為從第一層開始、只往下展開使用者還不懂的那部分，每層都能單獨成立；②新增「講為什麼不只講是什麼」一節；③圖表清單補時間軸、縮排樹、流程圖三種，並加上「能用圖講就不要貼程式碼」。自檢擴為十四題（含 Codex 指出的既有漏檢：換字不是重貼、證據不簡化、講到所以呢三節原本無對應自檢題）。

## 0.2.0 — 2026-09-05

- 首次實戰即失敗：重講版比原版更長，使用者當場指出「越寫越落落長」。真因是原規則只管句子長度不管版面密度，且「寧可寫十個短句」直接鼓勵膨脹。修正：第 2 條改為「同樣的內容要佔更少版面」並列出五種該砍的無資訊贅物（**不設字數或行數上限——使用者明確否決，事實一件都不能少，砍的是廢話不是內容**）；新增第 2-1 條「能畫就別寫」把 ASCII 圖表列為主力手段，附判準表、樣板、以及**圖表左右並排**的要求（終端機很寬，右邊留白是浪費）；「講詳細」段改為「放寬長度但不放寬不准廢話」；自檢擴為八題，前四題是寬度、密度、圖表、並排。
- 補寬度基準 170 與程式排版骨架。真因：連續四次手排爛版（框線折行、橫線接點差 2 格、中文寬度沒算）。查證過程：`tput cols`／`process.stdout.columns`／PowerShell `WindowSize.Width`／P/Invoke `CreateFile("CONOUT$")`+`GetConsoleScreenBufferInfo` 四法皆量不到 TUI 真實寬度（前兩者因 stdout 被 Claude Code 導管線而失效，後兩者讀到的是中間層 cmd.exe 的 console buffer 120），最後以階梯標尺請使用者目視確認寬度。

## 0.1.0 — 2026-09-05

- 建立。方法源自 mattpocock/skills productivity/wait-what；原版為英文四行提示，本版改中文情境，補上「換字不是重貼」「證據不簡化」「早就講過了」三條本地紀律。
